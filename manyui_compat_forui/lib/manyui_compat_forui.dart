/// Re-exports manyui's `M*` widgets under their forui `F*` names so an
/// existing forui codebase can migrate by changing a single import.
///
/// This is intentionally a thin shim — no behavior, just aliases.
library;

# manyui_testing

Test harness for [manyui](https://github.com/sneurlax/manyui). Add as a dev dependency:

```yaml
dev_dependencies:
  manyui_testing: ^0.1.0
```

Provides `pumpManyApp(WidgetTester, Widget, {MThemeData? theme, Size? viewport, MInputModality? modality})`.

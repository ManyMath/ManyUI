name: manyui_testing
description: Golden test harness, finders, and the pumpManyApp helper for manyui. Dev dependency only.
version: 0.1.0-dev
homepage: https://github.com/sneurlax/manyui
repository: https://github.com/sneurlax/manyui

environment:
  flutter: ">=3.27.0"
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  manyui: ^0.1.0-dev

dev_dependencies:
  flutter_lints: ^5.0.0
